import { makeStyles } from '@material-ui/core/styles';

const TaskViewPageStyles = makeStyles((theme) => ({
  container: {
    paddingTop: '10px',
    paddingBottom: '50px',
  },
}));
export default TaskViewPageStyles;
