import { makeStyles } from '@material-ui/core/styles';

const CategoryViewPageStyles = makeStyles((theme) => ({
  container: {
    paddingTop: '10px',
    paddingBottom: '50px',
  },
}));
export default CategoryViewPageStyles;
